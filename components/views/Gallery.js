export default st => `
<section>
export default () => `
<section id="Gallery">
</section>
`;

// <section>
// ${st.pics.reduce(
//   (html, Photogallery) =>
//     html + `<img src="${Photogallery.url}" alt="${Photogallery.title}">`,
//   ``
// )}
// </section>
// `;
